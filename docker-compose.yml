version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - DATABASE_URL=sqlite:///crm.db
      - SECRET_KEY=your-secret-key-here
    volumes:
      - .:/app
    command: python backend/app.py
